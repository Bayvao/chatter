server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Client-side routing: unknown paths are React Router's to resolve, not 404s.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
